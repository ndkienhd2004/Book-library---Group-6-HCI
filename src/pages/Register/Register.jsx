import NavBar from "../../components/NavigationBar";
import PATH from "../../constants/path";

const Register = () => {
  return (
    <div>
      <NavBar currentPage={PATH.register} />
    </div>
  );
};
export default Register;
